# Pinned for reproducibility (Itay's instinct from PR#1: same input → same output).
# Python 3.11.  Install into venv_wc-summary-crew (see README).
pydantic-ai==1.104.0
fastapi==0.136.3
uvicorn==0.48.0
supabase==2.30.1
python-dotenv==1.2.2
