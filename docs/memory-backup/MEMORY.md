# WorldCup 2026 — Memory Index

## Project
Semi-public social predictions app for FIFA World Cup 2026.
Stack: **React + Vite** (inner pages) + Vanilla HTML/JS (landing) + Supabase + Claude API. GitHub Pages hosting.
Mobile-first, dark theme, users arrive via WhatsApp invite link.

## Audits
- [EF sync audit + skill](ef-sync-audit.md) — `/verify-ef-sync` read-only EF/cron/trigger audit; headline gap: ~100 real WC games unmapped (no api_fixture_id → no sync crons).
- [Trivia push bug](trivia-push-bug.md) — ✅ **FIXED** (verified 2026-05-31): `fn_notify_trivia()` body now wraps the push in `IF EXISTS (SELECT 1 FROM trivia_questions WHERE available_from <= now() AND available_until > now())`. No empty-day pushes possible.
- [AI-summary match-day boundary](ai-summary-matchday-boundary.md) — match "day" = 07:30 UTC (not 00:00) so US match-nights don't split; **6 places** (5 SQL + 1 frontend): fn_schedule_ai_summaries + nightly-summary EF + digest + dashboard RPC + get_group_summary_data (M110–M113) + AiFeed.jsx loadDailyStandings (Day-standings "Pts today" table was undercounting vs roast — main 801f2f8, gh-pages SW v31).
- AiFeed summary text hidden (2026-05-27, gh-pages SW v30) — `.af-card-header` was `position:sticky`+opaque inside `.af-card{overflow:hidden}` → pinned header overlapped/hid the roast while scrolling. Fix: removed sticky (normal flow). Do NOT re-add sticky there (in-code NOTE at css/style.css:1250). AiFeed-only CSS. main 09abd31.
- **DELIBERATE: push notifications optimized for mobile; desktop-asleep won't receive = accepted cost** (2026-05-27) — do NOT "fix" `TTL:60` / M104 keep-2. Desktop misses correlate with push FIRE HOUR (00:40 Israel→received vs 03:10/06:10→asleep), not just the code. Detail + WC implication in [pwa-notifications.md](pwa-notifications.md).
- [AI rank 1000-row bug](ai-rank-1000row-bug.md) — nightly-summary EF re-implemented leaderboard in JS, hit Supabase JS-client 1000-row cap (`predictions` had 1934 rows) → global ranks silently wrong in BOTH `display_data` (table) AND `input_json` (LLM). Fix 2026-05-28: EF v35 calls `get_leaderboard()` RPC (single source of truth); EF football-api-sync v36 + defensive `.range(0,99999)` everywhere else; M114 backfills history (verified Test3/27-5 Itay 1→2 etc.). Lesson: ask the DB if it has the function; only `.range()` for JS-driver loops.
- [WC2026 squad seed](wc2026-squad-seed.md) — ✅ FINALIZED 2026-06-07: all 48 teams = exactly 26 active (1248), FIFA-PDF verified. **Pass #2+#2b (2026-06-18/19, all 48 played): placeholders 48→26**, applied 31 id changes incl. the 14 pass-#1 collisions (technique: park the dup/holder on a fresh negative −101..−122, NEVER delete inactives). Tangles fully fixed incl. both halves: Brazil Danilo 618 / Danilo Santos 275170, Spain Joan García 182718 / Eric García 619, Panama José Luis 2979 / Tomás 57910, Iraq Hussein 145465 / Mohanad 154767; +Salem Al Dawsari 44340 (star FW), Azizbek Ganiev 73520, 6 mismatches. **Lesson: after a tangle reassign, re-scan with last-token fuzzy match** — displaced twin's id is usually in match data under a variant spelling. Remaining **26 placeholders = players never fielded yet** (auto-resolve when they play). Zero scoring impact. JSON via `scripts/regen-squads-json.cjs`. Resolution via [[lineup-resolve-runbook]].
- [Lineup resolve runbook](lineup-resolve-runbook.md) — per-game SQL playbook: identify placeholder candidates in game_player_stats → UPDATE to real api_id → CASCADE to top_scorer_pick. Run after every WC2026 match June 11–July 19.

## Frontend / API reference
- [Group-context UX (persist + finished chips)](group-context-ux.md) — Picks persists selected group via sessionStorage['picks_group'] (History→game→back returns to chosen group); Dashboard per-group chips now render on finished today-games too with ⭐/✓/✗ outcome marker (was !finished-gated). Frontend-only, live for all.
- [Game group-predictions reveal](game-group-predictions-reveal.md) — Game.jsx now lists all group members' picks after KO (implements CLAUDE.md §3); per-game lazy fetch scoped to ?group= (≤12 rows, dodges 1000-cap); persistent home for finished games (Groups focus drops them). Live for all, Game-CWDdyVUD.js.
- [game_events duplication risk (ACCEPTED)](game-events-duplication-risk.md) — re-syncing a game can double the scorer list when api flips player-name/minute format (onConflict key includes player_name+minute). **v15 fix NOT deployed by choice** — display-only, zero scoring impact (game_events used by no fn/view/trigger). Fix if it recurs: DELETE game's events + one clean re-pull. Riskiest: knockout ET-followup + retry syncs.
- [Bosnia team-name mismatch](bosnia-team-name-mismatch.md) — ✅ FIXED 2026-06-13: stats tables stored `Bosnia & Herzegovina` (api) vs canonical `Bosnia-Herzegovina` → Game stats column "—" + scorer flag missing. Fix A: prod backfill (game_team_stats/player_stats/events, 1/26/1 rows). Fix B: football-api-sync **PROD v14** — writeStats canon()-canonicalizes stored team to DB games name. 🔁 RECURRING (Bosnia→Cape Verde→DR Congo): canon() falls through to raw on no-match → durable fix lives in `TEAM_ALIASES` not canon(). **v15** cape verde islands, **v16** congo dr, **v17** czechia (Jun 15–18). Each = alias + prod rename-backfill (NOT re-pull). **v18 (Jun 19) = ROOT CAUSE of recurring Bosnia: `normalizeTeam` strips punctuation to EMPTY, so DB "Bosnia-Herzegovina"→"bosniaherzegovina" (hyphen fuses words) while api "Bosnia & Herzegovina"→"bosnia herzegovina" → never match → canon falls through. Bosnia = only hyphenated WC team. Fix = 2 aliases forcing both forms to "bosnia-herzegovina".** ⚠️ api INCONSISTENT — a team can store canonical on game 1, variant on game 2 (Czech). Eyeball each team's stats column EVERY game (next: Bosnia v Qatar Jun 24). ⚠️ local index.ts WIP rewrite lacks canon()+aliases — re-add when it lands.
- [Scorer flags by team + champion-row removed](scorer-flags-and-champion-row.md) — 2026-06-12 (a6cbf6d, deploy pending): Top 5 Scorers flag now `TEAM_CODE[p.team]` not exact player-name match (api stats spelling drifts from squad seed → SK/Czech flags dropped). Champion-form sub-row removed from Dashboard global leaderboard (it only showed once a champion had played → inconsistent). Both frontend-only.
- [Picks candidates 1000-row cap](picks-candidates-1000row-cap.md) — ✅ FIXED 2026-06-10 (SW v40, 9174ac3): top_scorer_candidates (1248 rows) truncated at 1000 → Brazil 21 not 26. **PROD has HARD PostgREST max-rows=1000; `.range(0,99999)` does NOT beat it** (verified: Range 0-99999 → Content-Range 0-999/1248) — MUST paginate or use RPC for >1000 rows. Corrects the "range everywhere" claim in [[ai-rank-1000row-bug]].
- [Top scorer position format](topscorer-position-format.md) — `top_scorer_candidates.position` MUST be full names (Attacker/Midfielder/Defender/Goalkeeper), NOT GK/DF/MF/FW. M128 JSON sync wrote short codes → Picks position filter (exact-match) silently broke. Fixed PROD 2026-06-07. Don't touch frontend filter; normalize data on every write.
- [Game events penalty + OG display](game-events-penalty-og-display.md) — Game.jsx shows (P)/(OG) (prod SW v38, 2026-06-02). Verified-live api-football field ref: missed pen = type"Goal"+detail"Missed Penalty"; shootout = comments"Penalty Shootout"; OG team=beneficiary. api PRUNES shootout events over time → capture at sync. Deferred: missed-pen capture + shootout P1/P2 (knockouts).
- [Manual data corrections playbook](manual-data-corrections.md) — api revises stats AFTER the one-shot KO+120 sync (never re-pulled). Scorer in game_events but goals=0 in game_player_stats → missing from Top Scorers. Fix the single scoring field by hand (UPDATE goals), NOT a re-sync (re-pull rewrites 47 cosmetic fields + risks game_events dup; only `goals` is functional). Verify api first via dev EF probe_stats. Log: docs/MANUAL_DATA_CORRECTIONS.md. Cases (2026-06-14): Khoukhi goal (Qatar v Switzerland); passes+xG NULL (Brazil v Morocco) — passes/xG compute later than score so often NULL at sync.
- [Missing-stats diagnostic model](stats-missing-diagnostic.md) — triage: ONE field/player missing = api timing lag (row under right name, field 0/NULL → manual UPDATE, [[manual-data-corrections]]); WHOLE team blank + other team fine = name mismatch (data in DB under variant spelling → rename+alias, [[bosnia-team-name-mismatch]]). Check DB before re-pulling.

## Supabase
- URL: https://ftryuvfdihmhlzvbpfeu.supabase.co
- Anon key: in js/supabase.js (safe to expose)
- Client pattern: `_supabase` from js/supabase.js (UMD CDN, window.supabase)

## Local dev
- [Local dev port = 4173 (vite preview), not 5173](feedback_local_dev_port.md) — user runs `npm run preview` to keep vanilla landing + React app coexisting correctly.
- [postbuild = Node script, NOT Unix cp](feedback_postbuild_node.md) — `scripts/postbuild.cjs` handles dist/ copy cross-platform. Old `cp` chain failed silently on PowerShell, caused Phase 4 deploy bug. Don't revert.
- [venues.js had 6 March-2026 playoff placeholders](feedback_venues_placeholders.md) — UEFA PO-A→Bosnia, PO-B→Sweden, PO-C→Turkey, PO-D→Czech Republic, IC PO-1→DR Congo, IC PO-2→Iraq. Frontend lookup is by team-name, NOT game_id, so DB team-name changes require venues.js update. Fixed in `e5d3724`.
- [gh-pages must NOT track dist/](feedback_ghpages_no_dist.md) — stale dist/ on gh-pages silently overwrites local dist/ on every `git checkout gh-pages` (because dist/ is gitignored on main, git overrides without warning). Caused broken localhost after every deploy. Fix: `git rm -r dist` on gh-pages once. Verified clean post `303b25e`.

## Workflow rules
- [EF repo files ≠ source of truth](ef-repo-not-source-of-truth.md) — local `supabase/functions/**/index.ts` are NOT in sync with deployed EFs; **deployed PROD EFs = source of truth**. football-api-sync: prod v17 / dev v39 / repo = uncommitted WIP rewrite (matches neither). Always `get_edge_function` live → edit that → deploy → diff-verify. Never deploy the repo file assuming it's current.
- [Never CREATE OR REPLACE FUNCTION blindly](feedback_create_or_replace_function.md) — always dump current body via pg_get_functiondef FIRST, edit minimally; M123 destroyed M52's contrarian auto-predict by rewriting the body from scratch; restored by M125 same day.
- [Solo-user UI gaps](feedback_solo_user_ui_gaps.md) — DB layer supports solo users (group_id=NULL via M36/M122/M125) but frontend keeps gating UI on `groups.length > 0`. Dashboard.jsx 9174ac1 fix: render "Personal" pseudo-group chip when groups empty. Audit any group-aware UI for the solo path.

## Operations
- [DEV silenced during prod live](dev-env-silenced-during-prod.md) — 2026-06-13: dev `notify-admin` 4 triggers DISABLED (`tgenabled='D'`; re-enable via ALTER TABLE ENABLE TRIGGER). No active EF-calling crons on dev (sync/push/AI/digest all off); only DB-only auto-predict + cleanup left. sync-odds cron-job.org no longer points at dev. Gotcha: can't `UPDATE cron.job` on Supabase — use `cron.unschedule`/`cron.alter_job`.
- [Group member cap 10→12 (M129)](group-member-limit-12.md) — DB-only change 2026-06-11; enforced ONLY in `join_group` RPC (`v_count >= 12`); both envs md5-identical. Frontend `/10` labels (Groups.jsx 255/297/449/620/954 + HowToPlay.jsx:97) still say 10 — cosmetic, pending.
- [Knockout insertion = Option C (per-round, NOT up-front TBD)](feedback_knockout_insert_strategy.md) — insert each KO row with real teams + api_fixture_id at INSERT time so trg_auto_schedule_game creates all 4 crons cleanly. Insert EARLY (matchup-known time, not round-end time). Option A (UPDATE trigger) documented as backup.
- [Rosters/odds = bulk seed, NOT per-fixture](feedback_rosters_and_odds_pull.md) — `teams` + `top_scorer_candidates` are static M45 SQL seed (no API pull); `champion_odds` from sync-odds EF (TheOddsAPI/William Hill); `game_odds` from football-api-sync mode=sync_af_odds (Bet365). EF `setup_lineups` is per-fixture enrichment only.
- [AI summary per-group cron gap (M116 fix)](ai-summary-group-cron-gap.md) — empirically verified: fn_schedule_ai_summaries only fires on game INSERT, not on group/member INSERT. Fix = daily safety cron `ai-summary-schedule-daily` at 06:30 UTC. Critical for prods seeded before users.
- [Dev↔prod parity audit (2026-05-30) + dashboard-DDL rule](dev-prod-parity-audit-2026-05-30.md) — Full surface diff after cutover; only real gap was `player_tournament_stats.security_invoker` (M117 closes it). **Rule:** never apply DDL via dashboard alone — always `apply_migration` so `schema_migrations` tracks it and pg_dump captures it cleanly. M95/M96 + player_tournament_stats fix all bit us because they were dashboard-direct.
- [Knockout fixes sprint plan (Jun 15-22)](knockout-fixes-plan.md) — 4 knockout-only bugs found 2026-05-30 (PSG-Arsenal test): event mis-classification, ET display, AI summary timing, dashboard visibility. All gated on `phase<>'group'` → zero group-stage risk. Dev first, validate against PSG-Arsenal, promote to prod. Full plan: `docs/PLAN_KNOCKOUT_FIXES.md`.

## Prod Cutover (2026-05-30 → 2026-06-02)
- [Prod cutover plan](../../../../Desktop/World_Cup_APP/docs/PLAN_PROD_CUTOVER.md) — 7-phase rollout to pickyguessers.com. Pg_dump strategy (not file replay). Stop-verify gates V0-V19.
- [Supabase projects reference](reference_supabase_projects.md) — DEV `ftryuvfdihmhlzvbpfeu` (Tokyo). PROD `pickyguessers-prod` (Frankfurt, TBD). Always pass `project_id` explicitly.
- [Prod artifacts locations](reference_prod_artifacts.md) — Backups outside repo at `Desktop\wc2026-backup-2026-05-30\`. Rollback tags `pre-prod-cutover-2026-05-30` + `-ghp`. Migration folders: `supabase/migrations/` (both), `migrations-dev/` (dev only), `migrations-prod/` (prod only). Full inventory in `docs/PROD_FILE_INVENTORY.md`.
- [Supabase = source of truth](feedback_supabase_source_of_truth.md) — When local files disagree with DB, trust the DB. 86 DB-tracked vs 120 local files have name drift / bundled splits / dashboard-applied gaps. Don't replay files for initial prod build — pg_dump dev → apply to prod.

## Active Phase
✅ **LIVE ON GITHUB PAGES** — App deployed 2026-04-05 and verified working end-to-end.
✅ **nightly-summary EF ACTIVE + live tested** (2026-04-12, 5 groups processed, real La Liga/PL data, cron bug fixed).
✅ **Prompt v10 ACTIVE** (2026-04-10) — iterated v1→v10, all test CSVs in test/prompts/prompt_v*.csv.
✅ **Cron bug fixed M56** (2026-04-12) — `fn_schedule_ai_summaries()` body was `::text` not `jsonb`; silent failure; fixed.
✅ **Cron ::text bug re-introduced + fixed M74** (2026-05-12) — M73 rewrote `fn_schedule_ai_summaries()` and re-introduced the `::text` cast. All May 11 per-group cron jobs fired but failed silently. Fixed in M74. May 11 backfilled manually for all 8 groups. May 12 cron jobs regenerated.
✅ **Cron timing fixed M83** (2026-05-14) — M73 also regressed delay from 150→110min. Score sync runs at KO+120min; 110min fires before scores land → `games_not_finished`. M56/M59 correctly used 150min. Fixed in M83. May 13 backfilled manually. May 14 cron jobs updated to 22:00 UTC (19:30 last KO + 150min). fn_schedule_ai_summaries() canonical correct values: **150min delay, jsonb body (no ::text), per-group loop**.
✅ **feature/nightly-summary merged to main** (2026-04-12).
✅ **Groups focus-game fix** (2026-04-12) — now shows ALL live games regardless of KO time (not just exact same KO). Max 4 simultaneous (WC Jun 27). Query limit raised to 5.
✅ **Game page + Dashboard UI fixes** (2026-04-13) — 3 fixes: (1) LIVE badge on Dashboard (time-based: after KO + score=null + within 120min); (2) W/D/L form badges in Tournament Stats (colored, chronological order); (3) All-groups predictions on Game page — each group shows own pick, Predict/Edit per group, form labeled with group name.
✅ **AI Feed Total standings** (2026-04-21) — Total standings toggle per summary card (group rank + total pts). Global rank computed in EF v14, stored in ai_summaries.display_data (never sent to LLM). Auto-hidden on old summaries (display_data null), auto-appears on new ones. M57 deployed.
✅ **AI Feed daily + total standings fixes** (2026-04-22) — Daily: shows ALL group members incl. 0 pts / no prediction. Total: global rank now per (user × group) matching actual leaderboard. EF v15 deployed. M59 reverts M58 (auto-predict stays in fn_schedule_auto_predictions).
✅ **Global rank tiebreaker fix** (2026-04-22) — EF v16: global rank now sorts by total_points DESC, exact_scores DESC — matches leaderboard RPC exactly. Applies to new summaries only.
✅ **Auto-predict + AI summary scheduling clarified** (2026-04-22) — `fn_schedule_auto_predictions()` covers ALL games (no filter). `fn_schedule_ai_summaries()` covers ALL games. Call BOTH whenever new games are added to the DB.
✅ **In-app feedback system** (2026-05-03) — Hebrew modal, issue/idea + low/medium/high priority, optional screenshot upload to Supabase Storage. M60 deployed. See `memory/feedback-feature.md`.
✅ **Admin email notification system** (2026-05-03) — Resend API via notify-admin EF. Immediate alerts: new user, feedback, AI summary failure, EF crash/quota/stats errors. Daily digest 08:00 UTC: per-game stats, tokens, users, feedback, EF errors, app usage. M61 deployed. See `memory/admin-notifications.md`.
✅ **App usage tracking** (2026-05-03) — app_events table + 15s heartbeat (pauses when tab hidden). logEvent() fires page_view on all 5 pages; prediction_submit on Game; pick_submit on Picks. useHeartbeat in App.jsx.
✅ **Judge LLM system v3** (2026-05-04) — 5-agent parallel (v11-main-2/v12-picks-2/v13-unique-2/v10B + v10-baseline) + gpt-4o judge. M62+M63+M64+M65 deployed, nightly-summary EF v24 (Supabase v28) ACTIVE, notify-admin EF v3 live. 4 comparison runs completed (best: 16-25-10 CSV, 8.04 avg WTotal). PR #1 merged 2026-05-05. See `memory/judge-llm.md`.
✅ **Judge + v13 fine-tuning** (2026-05-06) — M72: v13 template opener ban. EF v24: judge today_pts vs total_pts check + reasoning must quote winner verbatim. Reviewed 2026-05-04 live run (20 candidates, 4 groups). See `memory/judge-llm.md`.
✅ **Migration alignment** (2026-05-05) — 6 stub local files for MCP-applied migrations. 73 local migration files total, all aligned with DB. supabase/CLAUDE.md is canonical log.
✅ **Auto-schedule trigger M68** (2026-05-04) — `trg_auto_schedule_game` AFTER INSERT on games auto-calls fn_schedule_auto_predictions + fn_schedule_ai_summaries + fn_schedule_game_sync. No more manual scheduling after game inserts. Exception handlers prevent INSERT failure on cron errors. fn_schedule_game_sync skipped if KO in past or api_fixture_id null (call manually after setup mode for those cases).
✅ **Backfill sync crons M71** (2026-05-05) — one-time loop calling fn_schedule_game_sync for all existing future games with api_fixture_id. Fills the M68 gap for pre-existing games. M68 + M71 = full cron coverage for all games existing + new.
✅ **winner_score + version_tag M69 + EF v23** (2026-05-04) — M69: ai_summaries.winner_score (judge total of winning agent); backfills from ai_judge_runs; also backfills version_tag into ai_judge_runs.candidates JSONB. EF v23: candidates JSONB includes version_tag; ai_summaries upsert writes winner_score. Deployed Supabase v27. Committed + pushed to main.
**Pending:** Verify custom Resend domain (currently sending from onboarding@resend.dev sandbox — may go to spam). Update FROM_ADDRESS in notify-admin EF once domain verified.
✅ **PR #1 merged** (2026-05-05) — feature/judge-llm merged into main via local merge (-Xours, main's v23 EF kept). PR closed.
✅ **Registration freeze + invite auto-join fix** (2026-05-05) — `js/auth.js`: await create_profile (1.5s max), pass invite code in redirect URL. `Groups.jsx`: auto-join silently on load, already_member shows friendly toast, errors open dialog fallback. `App.jsx`: welcome toast via wc2026_welcome flag. See `memory/auth-registration-fix.md`.
✅ **Nightly-summary per-group cron fix** (2026-05-10) — Root cause: TIMEOUT_MS=120s, 8 qualifying groups × ~25s = ~200s needed → 3 newest groups (Demo, Kanta Bayam, cheaters) always missed. Fix: M73 + EF v25 (Supabase v29). fn_schedule_ai_summaries() now creates one cron job per qualifying group per date (`ai-summary-{date}-{group_id[:8]}`). EF accepts group_id in body — single-group mode skips loop; no group_id = legacy loop for manual/test. May 9 backfilled for all 8 groups. May 10 has 8 per-group cron jobs at 20:50 UTC.
**Next:** Clean up test data (The Legends group + test games/fake scores), run both scheduling functions after real WC games seeded.
✅ **Feedback review completed** (2026-05-11) — 11 user feedback issues reviewed; 9 fixed, 1 deferred (PWA), 1 kept as-is. See `memory/feedback-review-2026-05-11.md`.
✅ **Share buttons + standings UI** (2026-05-11) — ↗ Share on AiFeed Day/Total standings tables + Groups leaderboard + Game predictions. Total standings columns reordered (Player→Pts→Grp→Global); name truncates. Fixed `group_rank` field name bug (was `rank`). See `memory/feedback-review-2026-05-11.md`.
✅ **Invite button → platform-aware** (2026-05-12) — Mobile: opens WhatsApp with pre-filled message + production GitHub Pages URL (shows link preview card). Desktop: copies invite link to clipboard. Pure frontend change in Groups.jsx `copyInvite()`.
✅ **Daily digest enhanced** (2026-05-12) — M76 + notify-admin v4: per-game manual/auto split, auto_exact/auto_correct, outcomes (Exact/W/D/L/Wrong) from total, share_click tracking in app_events, peak hour in Israel time (Asia/Jerusalem), v10B wins row added.
✅ **Trivia page** (2026-05-12) — M77 schema, M78 RPC, M79 deferred leaderboard, M80 40 questions seeded (Jun11–Jul20, 22:00 IL). Frontend: Trivia.jsx, BottomNav 5th tab, App.jsx route, analytics. Tested end-to-end on desktop + mobile. **Pending before go-live:** (1) delete test questions — May 12 + 6 `[TEST]` rows May 18–23; (2) guard `fn_notify_trivia()` with live-question check — `trivia-push-daily` cron (`0 19 * * *`, no end date) pushes unconditionally → empty-page pushes before Jun 11 AND forever after Jul 20; keep as-is while testing EFs, apply guard before launch. Apply M79 at tournament end. See `trivia-feature.md`.
✅ **How to Play guide** (2026-05-14) — HowToPlay.jsx modal + gold ⓘ CSS icon in Layout header on all pages. Deployed to gh-pages. Updated Picks section (2026-05-14): two-tab framing (Picks/Predictions), Upcoming + History sub-views, Groups desc adds "predictions". Deployed `0fb1296`.
✅ **Daily digest predictions + picks metrics** (2026-05-17) — M84–M90 + notify-admin EF v9. Predictions row: `total · edits · users · games · groups` from `predictions` table (per user per group, manual only). Champion pick + Top scorer pick rows: `total · edits · users · groups` from their tables. Replaces old app_events click counts. Committed `78af82f`.
✅ **PWA manifest + service worker + push notifications** (2026-05-17) — manifest.json + icon-180.png + icon-512.png (⚽ PNG, dark bg). sw.js: handles install/activate/push/notificationclick. App.jsx: NotificationPrompt (10s delay, asks permission, saves subscription); InstallBanner (Android install button / iOS Share hint — shows on iOS regardless of notifGranted). VAPID public key hardcoded; private key in Supabase vault as `Notification_Key`. push_subscriptions table (M91). fn_notify_ko + fn_schedule_ko_notification + fn_notify_trivia + fn_notify_ai_summary (M92/M93). send-push EF v6 (uses npm:web-push, handles 410 Gone, icon-notif.png + icon-badge.png with ?v=3 cache-bust). trivia-push-daily cron at 0 19 * * * UTC. trg_notify_ai_summary AFTER INSERT on ai_summaries. 102 KO notification crons backfilled (2026-05-17 M92). M98: games.phase CHECK adds 'friendly'. M99: trg_auto_schedule_game now auto-creates ko-notif cron on INSERT (covers all future games). **Known:** Android FCM endpoint confirmed; iOS push requires standalone PWA + iOS 16.4+. See `memory/pwa-notifications.md`.
✅ **Predictions Upcoming/History split** (2026-05-14) — Predictions tab now has Upcoming (unscored games, flat date order) and History (finished games, flat list) sub-tabs. History shows Exact/Correct/Wrong outcome badges per game. Sort controls: Date (newest↓/oldest↑) and Points (best↓/worst↑), toggle direction on re-click. Pure frontend change in Picks.jsx + css/style.css. Deployed to gh-pages. Updated 2026-05-18: Upcoming changed from phase-grouped to flat date order (closest first), all phases including 'friendly' now shown. Updated 2026-05-22: `predSubTab` (upcoming/history) + `historySort` ({by,dir}) now persisted to sessionStorage (`picks_predsubtab`, `picks_historysort`) — same pattern as `picks_tab`. Tapping a game then Back returns to the exact sub-view + sort instead of resetting to Upcoming/Date. gh-pages SW v23.
✅ **ET/Penalties stats fixes** (2026-05-13) — M82: `team_tournament_stats` view fixed — W/D/L now uses `knockout_winner` for ET/PEN games (was 90-min only); avg_goals uses `COALESCE(et_score, score)` for full-match count. Game.jsx record badges: now show `W·ET`, `W·P`, `L·ET`, `L·P` for ET/penalty outcomes. CSS badges widened to fit. My Stats (Exact%/Predicted%/Streak) unchanged — correctly based on 90-min (points are 90-min). Match stats from API already covered full 120-min (stats fetched after game confirmed finished).
✅ **Trivia security hardening M94** (2026-05-17) — `trivia_secrets` table (RLS enabled, no SELECT policy) stores `correct_option` + `explanation`. Dropped those cols from `trivia_questions`. RLS time-locked: `available_from <= now()` — future questions invisible. `submit_trivia_answer` (SECURITY DEFINER) joins `trivia_secrets` server-side; frontend unchanged.
✅ **Security RLS fixes M95** (2026-05-18) — `feedback`: added service-role-only SELECT policy (admin only). `ai_judge_scores` view: revoked SELECT from anon+authenticated (internal data). `team_tournament_stats` view left as-is (frontend uses it, public data).
✅ **Security M96** (2026-05-18) — `feedback_readable` view: revoked SELECT from anon+authenticated. `prompt_versions`: dropped anon read policy (service role only — EFs use service role).
✅ **Security M97** (2026-05-18) — `team_tournament_stats` view: recreated with `security_invoker=true` (was SECURITY DEFINER — ran as view creator, bypassing RLS).
⚠️ **iOS UI tweaks attempted + reverted** (2026-05-19) — Tried `.ios .prof-sheet { min-height: 65vh }` for sheet sizing consistency and `.standalone.ios .bottom-nav::before { top:8px }` to move bottom-nav line down. User reported no visible change on device after refresh — reverted in commit `43b4dd4`. Reinforces existing rule from `feedback_sw_deploy.md`: do NOT tweak iOS PWA nav lightly. Two known minor visual issues remain as-is: (1) iPhone profile sheet may render visually smaller than Android even with matching content (WebKit compactness), (2) iPhone PWA bottom-nav grey line sits at very top edge (not slightly inside). Both cosmetic only, not breaking.
✅ **Trivia cross-device result fix** (2026-05-19) — M100: `get_my_trivia_result(question_id)` RPC (SECURITY DEFINER) returns `correct_option` + `explanation` from `trivia_secrets` ONLY if the user has already answered (server-side `EXISTS` check on trivia_answers). Trivia.jsx calls this RPC as fallback when localStorage is empty (e.g., user answered on mobile, opens desktop — the secret was previously only in the device's localStorage from the one-time `submit_trivia_answer` RPC response). Cross-device now shows correct ✓ + explanation. No new attack surface — RPC enforces same `answered first` rule as `submit_trivia_answer`. gh-pages SW v15.
✅ **Trivia time-window query fix** (2026-05-19) — Trivia.jsx was using `.eq('question_date', today)` with `todayUTC()` for date matching, which only allowed questions visible until UTC midnight (=03:00 IL). Israel users had 5h window instead of intended 24h. Fixed by replacing with `.lte('available_from', nowISO).gt('available_until', nowISO).order('available_from', desc).limit(1).maybeSingle()`. Now question stays visible for the full 24h `available_from → available_until` window regardless of timezone. gh-pages `736b0a7` (SW v14).
✅ **Admin Test Mode toggle** (2026-05-19) — Dashboard profile sheet shows "Admin" section ONLY for `user.email === 'itayavioz1@gmail.com'`. Toggle button flips `localStorage.wc2026_test_mode='1'` ↔ removed. Trivia.jsx checks the flag and bypasses TOURNAMENT_START gate (`today < TOURNAMENT_START && !isTestMode`). Pattern reusable for future features: wrap with `if (isTestMode || real_condition)`. Invisible to regular users — only the admin email sees the toggle. gh-pages `c6df945` (SW v13).
✅ **SW update toast + refresh button + automated deploy** (2026-05-19) — Version detection via inline `window.__APP_VER__` injected into app.html at deploy time (deterministic, no SW timing race — earlier `controllerchange` and SW `postMessage` approaches both failed due to race between v_old being controller when `ready.then()` resolved vs v_new taking over via skipWaiting). App.jsx reads window var synchronously on mount, compares with `localStorage.wc2026_sw_ver`, shows toast. "Refresh to latest version" button in profile sheet (Dashboard ⚙️ icon). **Automated deploy**: `npm run deploy` (scripts/deploy.cjs) bumps SW_VERSION, saves dist files to OS temp BEFORE branch switch (avoids stale dist on gh-pages), verifies app.html has manifest/apple-touch-icon/SW/JS, injects `window.__APP_VER__`, commits + pushes gh-pages, handles stash pop conflicts gracefully. Also fixed pre-existing app.html bug: manifest, apple-touch-icon, apple-mobile-web-app-capable, SW registration script all missing on gh-pages (caused no install prompt on Android/Desktop + wrong icon on iPhone). gh-pages `37f37d2` (SW v12). Note: PowerShell does NOT support `&&` — must run `npm run build` then `npm run deploy` separately.
✅ **iOS PWA UI fixes** (2026-05-18) — R1: `.ios` class, gold tap, nav height 88px. R2: `.page-nav` line via `::after`, `.standalone.ios` selector. R3: hover gated by `@media(hover:hover)`, `box-shadow` for page-nav line, `height:calc(56px+env())`. R4: nav balance (`padding-bottom:8px` not env); `#globe-container` background `#0a0a0a` + CDN swap jsdelivr-first; send-push EF v7 default URL adds project subpath. R5: kill `backdrop-filter` on `.ios .page-nav` (WebKit bug 158807). R6: `.ios { --border: rgba(255,255,255,.18) }` brightens dividers; `.standalone.ios .bottom-nav padding-bottom:24px`; auth2.js hoists nav-login-btn handler before session await. R7: standalone bottom-nav padding 24→40px. R8: bottom-nav 40→48px + force-fresh push subscription on every PWA open (unsubscribe+subscribe destroys 410-Gone cached endpoint, esp. for Android 13+ POST_NOTIFICATIONS coin flip) + 24h re-ask cooldown (replaced permanent skip flag — gives users second chance after PWA reinstall). gh-pages `cfc2e7c`. See `memory/ios-pwa-fixes.md`.
✅ **AI summary push consolidated to 1 per user/night** (2026-05-20, M101) — Dropped `trg_notify_ai_summary` trigger + `fn_notify_ai_summary` (per-group push on INSERT — user in N groups got N pushes). Added `fn_notify_ai_summary_daily(date)` = DISTINCT union of all qualifying group members for that date → 1 push per user. Patched `fn_schedule_ai_summaries` to also schedule `ai-summary-push-{date}` cron at last_KO+160min (10min after per-group AI generation jobs at +150min). 35 push crons registered through 2026-07-19. Non-group users never receive push (no group_members row). Push URL still `/ai-feed` (lands on first group). See `memory/pwa-notifications.md`.
ℹ️ **AI push timing clarified** (2026-05-21) — +160 is correct as-is. +150 = per-group OpenAI jobs fire (parallel, ~25-30s each, internal retry 2×5s). +160 = consolidated push reads `ai_summaries` and sends. 10-min gap prevents race condition where push reads empty table before slowest group's row lands. EF internal retry only; no cron-level retry.
✅ **Prediction edit log M105** (2026-05-22) — Daily digest's "edits" only counted cross-day modifications (submitted prior day, changed yesterday) AND max 1 per prediction (table stores final row only) → same-day submit+edit always showed edits=0. Fix: `prediction_edit_log` append-only table + `trg_log_prediction_edit` AFTER INSERT/UPDATE on predictions logs EVERY manual value change (is_initial flag; `IS DISTINCT FROM` check ignores score-sync `updated_at` bumps). `fn_daily_admin_digest` prediction block now reads the log: pred_total=new submissions, pred_edits=every edit action (same-day + multiple-per-pred counted). notify-admin EF unchanged (same keys). Log starts now — no backfill (no edit history existed). Tested: 2 edits → 2 rows. Pure DB, no frontend, no SW bump. NOT related to perf cache bug (that was display-only). See supabase/CLAUDE.md M105.
✅ **Pick edit log M106** (2026-05-22) — Same as M105 for champion + top scorer picks. Generic `pick_edit_log` (pick_type, value, is_initial) + `trg_log_champion_pick_edit` (team changes) + `trg_log_top_scorer_pick_edit` (player_name changes). Digest champ/scorer blocks repointed at it. Only meaningful until June 11 pick deadline (picks lock after → edits always 0). Pure DB, no frontend.
✅ **Cache-staleness loop CLOSED — all cross-page writes clear-all** (2026-05-23) — Systematic sweep of all 23 writes vs the 3 DataCacheContext keys (`dashboard:`, `groups-list:`, `group-lbs:`). 6 rare writes touched data shown on multiple cached pages but cleared only one cache → fixed all to `cache.invalidate()` (clear-all): (1) Dashboard username rename (username in all 3 caches), (2-4) Groups create/join×2/rename (new group/name missing from Dashboard My Stats), (5) Trivia submit (after the final, M81 guard lets trivia points enter leaderboards → answering left Groups stale). **Game.jsx prediction edit stays TARGETED (dashboard-only)** — it's the hot path AND predictions are 0 pts pre-kickoff so they never change the leaderboard from user action (points come from server-side scoring when game already locked). Picks.jsx already clear-all (prior fix). **Principle: rare cross-page writes → clear-all; hot same-page writes → targeted key.** No cache impact (verified): push_subscriptions, app_events, feedback, create_profile, delete_account. gh-pages SW v26. This is the final correction loop — every write traced.
✅ **Groups leaderboard staleness after pick save fix** (2026-05-23) — Picks.jsx only invalidated `dashboard:` cache after champion/top-scorer/prediction save, but those values also appear in the Groups per-group leaderboard cache (`group-lbs:{ids}`, 60s TTL) → Groups showed old pick up to 60s. Champion-vs-top-scorer / per-platform asymmetry users reported was a RED HERRING: both come from the SAME cached `get_group_leaderboard` RPC (one blob), so always equally stale — apparent difference was 60s-cache random timing + champion renders as flag IMAGE (CDN load) vs top scorer as text. Fix: 3 `cache.invalidate('dashboard:'+user.id)` → `cache.invalidate()` (clear all) in Picks.jsx. Pick saves are rare so clear-all is cheap. gh-pages SW v25. Lesson: any value shown in BOTH Dashboard and Groups leaderboards needs full cache clear, not just dashboard key.
✅ **Un-lazy Dashboard — iOS flag flicker fix** (2026-05-22) — Root cause of the iPhone Dashboard flag flicker: React.lazy (round-1 perf) made Dashboard a separate chunk loading AFTER the main bundle → flag `<img>` tags rendered later → iOS Safari delayed the cold flagcdn requests → flicker. Before the perf pass, Dashboard was in the main bundle and flags fetched early (no flicker). Fix: `import Dashboard` eagerly in App.jsx (it's the post-login landing page); other 5 routes (Game/Picks/Groups/AiFeed/Trivia) stay lazy + idle-prefetched. Main bundle 108→124KB gz — still below pre-perf 147KB, so all other gains retained. flagcdn preconnect (R2) still in place. gh-pages SW v24. Net vs before-round-1: still faster (smaller bundle via 5 lazy routes, 1 RPC dashboard payload, 60s cache, prefetch, WebP) + flags load instantly again. Groups/Game flags (other CDN: jsdelivr SVG) may still have brief first-load delay — deferred; SW flag-cache is the next option if needed.
✅ **Perf pass round 2** (2026-05-21) — 3 fixes on top of yesterday's perf pass: (1) **Dashboard cache invalidation gap fixed** — Game.jsx/Picks.jsx/Trivia.jsx now call `cache.invalidate('dashboard:'+user.id)` after every write (prediction submit, champion/top-scorer pick, trivia answer). Before: only username rename invalidated → Dashboard showed stale data for up to 60s after any change. 60s cache itself stays. (2) **Idle route prefetch in App.jsx** — after user auth, `requestIdleCallback` (or 2s setTimeout fallback) downloads Game/Picks/Groups/AiFeed/Trivia chunks in background. Next navigation = instant, no "load chunk on click" wait. Doesn't block first paint. (3) **Trophy.jfif (24KB) → Trophy.webp (13.8KB)** — 43% smaller hero image, used in landing + Dashboard. gh-pages SW v22.
✅ **flagcdn preconnect — iPhone flag flash fix** (2026-05-21) — Dashboard flags missing on iPhone first paint, only appeared after page navigation. Root cause: `flagcdn.com` was NOT in preconnect list (only Supabase + Google Fonts were). iOS Safari aggressively delays third-party image requests during initial paint when connection is cold — perf pass made this worse by adding React.lazy on Dashboard (chunk loads after critical resources, so flags render later, so iOS marks them low priority). Fix: 2 lines added to `src/app.html` and `index.html` — `<link rel="preconnect" href="https://flagcdn.com" crossorigin>` + `<link rel="dns-prefetch" href="https://flagcdn.com">`. gh-pages SW v21. Android/Desktop never had this issue (more permissive image loading).
✅ **iOS overnight push fix** (2026-05-21, send-push v8 + M104) — Root cause: APNS silently rotates PWA tokens every 1-2 weeks (no 410); no urgency header → iOS deprioritizes on locked overnight devices; sequential for-await may trip per-device throttle; M101 removed accidental 3x redundancy. **Fix**: v8 adds `{urgency:'high', TTL:60}` + `Promise.allSettled` parallel sends + bulk stale delete. M104 `fn_cleanup_push_subscriptions()` keeps latest 2 subs/user (Dani pruned 7→2) + daily cron 03:00 UTC. Same fix protects late-night WC KO pushes (~03:45 IDT for some games). M101 1-push architecture KEPT. Android FCM was always fine but fix doesn't hurt. Live test tonight at last_KO+160min. See `memory/pwa-notifications.md`.
✅ **Perf pass + deploy fix** (2026-05-21) — 5 zero-risk speedups: preconnect (#1), React.lazy on 6 routes (#4; main bundle 147→108KB gz, −26%), DataCacheContext 60s TTL on Dashboard+Groups (#2), `get_dashboard_payload()` M103 (#3; 13 calls→1), skeleton CSS (#6). SW (#5) skipped. **Initial deploy black-screened**: `scripts/deploy.cjs` only copied main JS+CSS, missing the 9 new lazy chunks → fixed to enumerate ALL `dist/assets/*`. Modulepreload tag removed (Vite serialized JSX). gh-pages SW v20 (good). See `memory/perf-pass-2026-05-21.md`.
✅ **Trivia: 40s timer + missed = wrong** (2026-05-20, M102) — Timer reduced 60s → 40s (Trivia.jsx CircleTimer default, useState, openQuestion, progress bar, "40 seconds" copy in idle + pre-tournament rules; HowToPlay.jsx line 153). Missed questions now auto-insert `trivia_answers(selected_option='miss', is_correct=false, points_earned=0)` at `available_until` via `fn_auto_miss_trivia(question_id)` + `trivia-miss-{id[:8]}` cron. `trg_schedule_trivia_miss` AFTER INSERT auto-schedules for new questions. Backfill: 42 future-dated questions (2 test + 40 tournament Jun 11–Jul 20) scheduled. Filter `auth.users.created_at <= available_from` so users who registered after the question expired aren't penalized. Push notification unchanged (1 daily broadcast via `trivia-push-daily`). See `memory/trivia-feature.md`.
✅ **Picks "Top 5 Scorers" card** (2026-05-24) — Picks.jsx `loadTournamentResults()` Top Scorer result card was showing ONLY players tied at the single highest goal count. Changed to top 5 scorers, **keep-ties**: takes scored players (`total_goals > 0`), cutoff = 5th-place goal count, includes everyone `>= cutoff` so a tie at 5th is never split (list may exceed 5 rows). Display shows standard competition rank (`findIndex` of first row at that goal count + 1 → tied players share a rank) via new `.pk-result-rank` CSS. Title → "⚽ Top 5 Scorers". **Query**: `player_tournament_stats` filtered server-side `total_goals > 0` (avoids PostgREST ~1000-row default cap — table has 1191 rows, only ~135 with goals) + no `.limit()` so a deep tie (e.g. 11 players on 3G → 13-row list) is never truncated. **Card height**: `.pk-result-card-scorers max-height` = `calc(4 * 1.25rem + 3 * .35rem)` = 6.05rem → shows exactly 4 rows then scrolls (rows are flag-height ~19px, NOT 2rem — an earlier `4 * 2rem`=9.05rem mistakenly showed ~6 rows). Pure frontend. gh-pages SW v29, main `321645b`.
**Ideas backlog:** Hebrew AI summary option, tight-gap metric in nightly-summary JSON. See `memory/feedback-review-2026-05-11.md`.

## Deployment
- Landing + register/login: https://itayavioz.github.io/2026-World-Cup-Social-Predicting-App/
- React app entry: https://itayavioz.github.io/2026-World-Cup-Social-Predicting-App/app.html#/dashboard
- GitHub Pages: `gh-pages` branch, `/ (root)` — **no GitHub Actions, fully manual**
- Architecture: vanilla `index.html` landing (intentional) → login → `app.html` React SPA
- [Deploy steps](feedback_deploy.md) — copy dist/ contents to ROOT of gh-pages (not dist/ subfolder)

## Feedback
- [Dev server auth limitation](dev-server-auth-limitation.md) — localhost:5173 has no login page (Vite root:src); auth flow + testing only works on gh-pages production
- [Commit means commit only](feedback_commit_no_push.md) — "commit" = local commit only, never push unless explicitly asked
- [API keys in vault only](feedback_api_keys.md) — keys live ONLY in Supabase vault, never ask for values, never put in frontend/.env
- [API stats null→0 fix](feedback_api_stats_null.md) — API Football returns null for offsides (and possibly others) when count=0; use `?? 0` for all count fields in writeTeamStats; gk_saves/gk_conceded pending bug
- [Odds sources](feedback_odds_sources.md) — game odds: API Football Bet365 only; champion odds: TheOddsAPI William Hill only; single source per data type
- [Deploy to gh-pages](feedback_deploy.md) — copy dist/ contents to ROOT, not dist/ subfolder; fully manual, no GitHub Actions
- [SW + deploy rules](feedback_sw_deploy.md) — never use clients.navigate() in SW activate; always verify app.html hashes after deploy; iOS startup cache requires PWA reinstall
- [team.html data duplication](feedback_team_html_duplication.md) — team.html has own hardcoded TEAMS+TEAM_EXTRA (independent of js/main.js); any team edit must be applied to BOTH files; approved fix: load js/main.js via script tag instead

## Trivia
- [Trivia feature](trivia-feature.md) — DB schema (M77/M78/M80), RPC, deferred leaderboard (M79), 40 questions seeded Jun11–Jul20, frontend ✅ built+tested 2026-05-12. M94: security hardened — answers in `trivia_secrets`, RLS time-locked.

## How to Play Guide
- [How to Play modal](how-to-play.md) — gold ⓘ icon in nav header (all pages), full scrollable guide modal, deployed 2026-05-14

## PWA & Notifications
- [PWA + notifications](pwa-notifications.md) — manifest, sw.js v8, VAPID keys, M91/M92/M93 schema, send-push EF v7 (default URL includes project subpath; icon-notif transparent ⚽ + icon-badge "WC" text, ?v=3 cache-bust), platform behavior, debugging silent push failures

## Phase Memory Files
- `memory/db-phase.md` — schema, migrations 1–25, RPCs, decisions, test pages
- `memory/edge-function-phase.md` — EF status, vault config, football-api-sync error handling audit (2026-04-11)
- `memory/frontend-phase.md` — React+Vite migration status, page/component build tracker
- `memory/qa-round2.md` — 6 edge-case bugs found + fixed (2026-04-01)
- `memory/qa-round3.md` — 8 issues: contrarian logic, RLS fix, auth param, max 3 groups, join deadline, ungrouped migration, stats, vault fix (2026-04-02)
- `memory/api-pull-plan.md` — API Football data pull plan: teams + players from API (not hardcoded) ← NEXT
- `memory/feedback-feature.md` — in-app feedback system: DB schema, RLS, storage, component, admin view
- `memory/admin-notifications.md` — admin email system: notify-admin EF, M61 tables/triggers, app_events, daily digest, pending domain setup
- `memory/judge-llm.md` — Judge LLM system: 5-agent (v11/v12/v13/v10B + v10-baseline), M62–M65 schema, EF v23 active, test results, PR #1 merged
- `memory/auth-registration-fix.md` — Registration freeze root cause + fix (2026-05-05): redirect immediately after signUp, welcome toast on first app load
- `memory/feedback-review-2026-05-11.md` — All 11 feedback issues: decisions + changes. Key note: landing page auth file is **auth2.js** not auth.js
- Point 7 (player rating): two ideas to decide — 1) Game page stat (players sorted by rating), 2) AI summary context (top 3 rated fed to prompt). Details in `memory/edge-function-phase.md`.

## Skills
- `/verify-feature [0-9]` — test runner: DB checks + browser test + feedback report + memory update ← USE NOW
- `/db-feature [name]` — ERD + RLS planner (SKILL.md = live ERD source of truth)
- `/frontend [page|file]` — full workflow: read → plan → fill gaps → build → update memory → link → auto-chains /ux ← READY
- `/ux [page|file]` — UX audit: mobile, visual, states, a11y, component reuse → prioritized report → ask before fix ← AUTO after /frontend
- `/edge-function [nightly-summary|football-api-sync]` — EF builder: pre-checks → questions → build → deploy → auto-verify → report → memory update ← ACTIVE
- `/tips` — Claude Code tips

## Dashboard
- [My Stats counting rules](dashboard-stats.md) — Exact%/Predicted%/Streak count from 2026-04-11 (test data exclusion), definitions, impl notes

## Structure
- [Project structure](project_structure.md) — reorganization decisions 2026-04-11 (what moved where, what was skipped)

## Key Files
- CLAUDE.md — app characterization + file structure
- docs/UX_PATTERNS.md — spacing grid, touch targets, state patterns, a11y rules, do/don'ts (used by /ux)
- .claude/skills/db-feature/SKILL.md — live ERD + RLS (always up to date)
- docs/PLAN_REACT_VITE.md — React+Vite migration plan (full build order) ← ACTIVE
- supabase/migrations/ — 100 local files (all deployed): ...102: trivia missed = wrong + 40s timer; 103: get_dashboard_payload RPC; 104: push_subscriptions cleanup; 105: prediction_edit_log; 106: pick_edit_log (champion+top scorer edit tracking)
- supabase/functions/notify-admin/index.ts — ✅ deployed v9 ACTIVE (2026-05-17) — v9: predictions total·edits·users·games·groups + champion/scorer picks total·edits·users·groups from DB tables; v5: fix W/D/L double-count
- supabase/functions/football-api-sync/index.ts — ✅ deployed v29 ACTIVE (v29: +reportEfError at quota/stats_write/crash; v24 base)
- docs/QA_REPORT.md — full QA report (22 initial issues + 6 edge-case issues, all fixed)
- supabase/functions/sync-odds/index.ts — ✅ deployed v20 ACTIVE (v20: +Bosnia & Herzegovina→Bosnia-Herzegovina name map; v19: +reportEfError at crash)
- docs/data/API_FULL_SUMMARY.csv — full API field verification (api-football + theoddsapi, all 7 sections)
- supabase/functions/nightly-summary/index.ts — ✅ deployed v25 ACTIVE (Supabase v29, 2026-05-10) — single-group mode (group_id in body); per-group cron arch (M73); see judge-llm.md for prompt details
- supabase/functions/send-push/index.ts — ✅ deployed v7 ACTIVE (2026-05-18) — v7: default URL includes /2026-World-Cup-Social-Predicting-App/ project subpath (was `/app.html#/dashboard` → SW resolved to github.io root → 404); v6: icon-notif + icon-badge ?v=3
- test/judge_compare.cjs — local 5-agent test script (use .cjs not .js); runs all 5 prompts on 4 dates, outputs CSV; needs OPENAI_API_KEY. 4 runs archived in test/
- supabase/CLAUDE.md — deployed migrations log + EF status + pending setup
- docs/PLAN_API_SYNC.md — EF architecture, cron lifecycle, post-deploy setup order
- docs/DATA_SOURCES.md — API field mappings (api-football.com + theoddsapi.com)
- docs/ERROR_HANDLING.md — error groups 1–7 (API sync) + A–D (nightly summary) + implementation status table (audited 2026-04-11) + recommended fix order
- js/main.js — TEAMS array + HOST_SCHEDULES (all 104 games) — to be extracted to src/lib/teams.js
